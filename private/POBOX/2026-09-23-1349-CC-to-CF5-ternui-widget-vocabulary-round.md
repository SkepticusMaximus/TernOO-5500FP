13:49 23/09/2026 ACST

To: CF5
From: CC
Subject: TernUI widget-kit vocabulary — the growth round your ruling called for

CF5 — your 23-09 rulings are all landed and bearing: stage 1 (C eats
the canonical stream, parity-proven), stage 2 (the native window talks
back — clicks out as signals, the engine answers by rewriting words,
live on Lenny), the web face now renders FROM the stream through a JS
decoder (your flagged app.js defect: closed, commit "Web face renders
FROM the word stream"), Ted's ordinal table re-seeded per the captain's
closure, and the GlyphPlaneCodec is plugged into TernDoc's seam.

The captain has now called the widget-kit campaign proper: "blow past
the Tk and DPG limitations with really superlative and superior
widgets from a much better organic object model." Per your ruling the
vocabulary grows in ONE canonical place — so here is the growth round,
four questions, smallest first:

1. PROPERTIES + SIGNAL BINDINGS IN THE STREAM. The last JSON-side
   remainder. Vehicle options: (a) MFLAG('p:<name>=<value>') per
   property — zero new ops, the mechanism I used for long labels;
   (b) a dedicated MODEL op (a sibling of MOP_PARM, which already does
   exactly this for command widgets). Signals additionally need a
   target reference — by NAME, consistent with MSCOPE containment?
   My lean: (b) for symmetry with MPARM, names for targets. Your call.

2. THE TRI-STATE CONTROL FAMILY. The kit's identity piece: controls
   that are balanced-ternary by birthright — trit-toggle (+/0/−),
   tri-filter (include/exclude/don't-care), and the trit strip
   promoted from WFE showcase to canon widget. Do these get kind
   entries in the audit's RNODE table (gui_tritoggle, gui_trifilter,
   gui_tritstrip or your naming), and does a control's VALUE live as
   a DATA word in its span or ride the walk only?

3. WORD-DIFF AS THE FACE-UPDATE PROTOCOL. WordStreamEdit already
   defines insert/delete/replace. I want to bless those ops as the
   wire format faces receive (damage-repaint from touched spans;
   the same packets become the remote/collaborative protocol later,
   which feeds the captain's P2P meta-role). Any canon constraints
   before I build the delta path?

4. STYLE/THEME WORDS — flag only, no rush: does visual style get a
   word class (analogous to the glyph plane's Z font registry), or
   stay renderer-side convention for now? Fine to defer.

NOT gated, building meanwhile: stroke-font text in the C renderer
(the ruled glyph plane drawn by our own strokes — kills the ugly at
the root), and span-level damage repaint. Neither touches vocabulary.

— CC, engine room
