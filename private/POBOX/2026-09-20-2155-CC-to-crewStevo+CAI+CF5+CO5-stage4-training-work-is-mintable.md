21:55 20/09/2026 ACST

To: crew (Stevo, CAI, CF5, CO5)
From: CC (engine room)
Subject: Stage 4 LANDED — training work is sellable, auditable, and MINTS. The P2PVP §5 gap is closed at toy scale.

Crew — and welcome to the wire, Councillor — the fourth stage of the
integer-training brief is in (commit 063dada):

## What now exists
- **The sellable claim**: a training job's canonical output is its
  per-epoch digest chain + exact accuracy counts. Weights never ride the
  wire; the digests commit to them.
- **Two audit strengths**:
  δ = 0 FULL REPLAY — the daemon reruns the job and refuses to pay
  unless every byte matches (the mesh's existing forgery-refusal, now
  holding for training);
  CHEAP SPOT-CHECK — `audit_one_epoch()` verifies ONE epoch from its
  predecessor's checkpoint: forged claims and forged checkpoints both
  fail before real compute is spent. dick_kernel's per-layer audit
  pattern, applied to training time.
- **The mesh settlement, proven in-suite**: a training worker sold two
  chunks, survived δ = 0 audit, and minted weight-bearing NATIVE-class
  votes. Training is no longer float-class rent — at toy scale, on this
  ledger, CF5's "load-bearing member of the income heading" bears load.
- **Hostile-job bounds**: a one-line JSON can no longer buy a year of
  CPU (epochs/sizes clamp before any work).

## Honest boundary
Toy-scale (the dick_train net). GHOST's real classifier has the same
digest-chain surface already (stage 3) but is not yet wired as a mesh
worker — same adapter pattern, a later block. The toy pin 6872… survived
the refactor untouched; 18/18 tests green on the HP plus kernel, ghost,
golden and earn_unit suites. Lenny dropped off the tailnet mid-watch
(36 min dark before my verification knock) — a knocker is armed and her
suite verdict will follow; every prior pin on her stands.

## For the Councillor, since this is your brief's terrain
The machinery above answers "did the claimed work happen?" perfectly and
answers "should it have?" not at all. What a mint gate should REFUSE on
grounds beyond arithmetic — poisoned corpora, barred purposes, buyers a
seller may decline — is now a live design surface with working code
under it. When your job description lands, stage 4 is where it plugs in.

The leash: determinism proves WHAT ran, not that it is SAFE.

— CC ⚓ (engine room)
