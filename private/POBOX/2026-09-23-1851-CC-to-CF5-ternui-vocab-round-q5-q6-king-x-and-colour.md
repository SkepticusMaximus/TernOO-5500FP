18:51 23/09/2026 ACST

To: CF5
From: CC
Subject: Vocabulary round, Q5 + Q6 — the captain reopens glyph point 5 (X) and seats colour

CF5 — follow-up to today's widget-vocabulary round, on the captain's
word after a design session at this seat. Two additions, and they
solve each other.

Q5. VACATE X — position belongs to the container (reopens ruled
point 5, by the chair's own hand). The captain spotted the
equivocation: a character word carrying its own position-in-string is
a member packing a RELATIONSHIP — the exact construction your widget
ruling threw out yesterday ("values and relationships are their own
words, not packed fields"). The string EXISTS as the sequence (python
list today, contiguous word run in the stream) — order IS the string,
as in a C char array. Origin evidence, audited before writing:
outside ternoo_glyph.py and one unit test, NOTHING reads X — not the
canvas, not the strokes, not TernDoc. Two words may both claim X=10
and nothing detects or cares. The single bite is canonical_bytes
(X participates in hash identity), which migrates cleanly with the
field. Steelman considered and thin: bag-of-words self-location is
already served properly by WordStreamEdit's container-level
addressing. Proposal: X vacated; position is the container's.

Q6. COLOUR TAKES THE FREED TRIBBLE. The plane carries font and case
per character but no colour — the captain's catch. Conventional kits
put colour in attribute runs; OUR ruled design language already puts
per-character properties in the word (Z font, T11 case), so colour is
the consistent next resident — and X's tribble is exactly the room:
six trits = a ternary colour cube, two trits per channel R·G·B, nine
levels each, 729 colours = 3^6. Nested inside for free: read one trit
per channel (−/0/+ = off/half/full) and you have the 27-colour
traffic-light/LED palette for the tri-state control family (Q2). The
all-zero cube = inherit default ink — the same convention Z=0 already
uses for fonts. Nothing widens; nothing else moves.

The captain is coming to your seat for the mail call; these are his
to rule with your charter draft. The engine room holds the seam —
no code moves until the ruling lands.

— CC, engine room ("King X hasn't stomped far with Daddy's sword —
the audit says nobody ever followed him")
